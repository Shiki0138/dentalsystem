<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-semibold text-gray-900">患者一覧</h1>
      <p class="mt-2 text-sm text-gray-700">
        登録済みの患者情報を管理します
      </p>
    </div>
    <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
      <%= link_to "新規患者登録", new_patient_path, 
          class: "block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold text-white hover:bg-indigo-500" %>
    </div>
  </div>

  <!-- 検索フォーム -->
  <div class="mt-8 bg-white shadow-sm rounded-lg p-4">
    <%= form_with url: patients_path, method: :get, local: true, class: "sm:flex sm:items-center" do |f| %>
      <div class="w-full sm:max-w-xs">
        <%= f.text_field :search, 
            placeholder: "名前、カナ、電話番号、メールで検索", 
            value: params[:search],
            class: "block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
            data: { turbo_frame: "patients" } %>
      </div>
      <%= f.submit "検索", class: "mt-3 sm:mt-0 sm:ml-3 inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700" %>
      <%= link_to "クリア", patients_path, 
          class: "mt-3 sm:mt-0 sm:ml-3 inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" %>
    <% end %>
  </div>

  <!-- 患者テーブル -->
  <%= turbo_frame_tag "patients" do %>
    <div class="mt-8 flex flex-col">
      <div class="-my-2 -mx-4 overflow-x-auto sm:-mx-6 lg:-mx-8">
        <div class="inline-block min-w-full py-2 align-middle md:px-6 lg:px-8">
          <div class="overflow-hidden shadow ring-1 ring-black ring-opacity-5 md:rounded-lg">
            <table class="min-w-full divide-y divide-gray-300">
              <thead class="bg-gray-50">
                <tr>
                  <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                    患者番号
                  </th>
                  <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                    氏名
                  </th>
                  <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                    カナ
                  </th>
                  <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                    電話番号
                  </th>
                  <th scope="col" class="hidden md:table-cell px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                    メール
                  </th>
                  <th scope="col" class="hidden lg:table-cell px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
                    最終来院日
                  </th>
                  <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-6">
                    <span class="sr-only">操作</span>
                  </th>
                </tr>
              </thead>
              <tbody class="divide-y divide-gray-200 bg-white">
                <% @patients.each do |patient| %>
                  <tr>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-900">
                      <%= patient.patient_number %>
                    </td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-900">
                      <%= link_to patient.name, patient_path(patient), 
                          class: "text-indigo-600 hover:text-indigo-900" %>
                    </td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                      <%= patient.name_kana %>
                    </td>
                    <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                      <%= patient.phone %>
                    </td>
                    <td class="hidden md:table-cell whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                      <%= patient.email %>
                    </td>
                    <td class="hidden lg:table-cell whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                      <% last_appointment = patient.appointments.maximum(:appointment_date) %>
                      <%= last_appointment ? l(last_appointment, format: :short) : "未来院" %>
                    </td>
                    <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
                      <%= link_to "編集", edit_patient_path(patient), 
                          class: "text-indigo-600 hover:text-indigo-900" %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    <!-- ページネーション -->
    <% if @patients.total_pages > 1 %>
      <div class="mt-4 flex items-center justify-between">
        <div class="flex flex-1 justify-between sm:hidden">
          <%= link_to_prev_page @patients, "前へ", 
              class: "relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
          <%= link_to_next_page @patients, "次へ", 
              class: "ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
        </div>
        <div class="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
          <div>
            <p class="text-sm text-gray-700">
              全<span class="font-medium"><%= @patients.total_count %></span>件中
              <span class="font-medium"><%= @patients.offset_value + 1 %></span> -
              <span class="font-medium"><%= @patients.offset_value + @patients.length %></span>件を表示
            </p>
          </div>
          <div>
            <%= paginate @patients %>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>

  <!-- CSVエクスポート -->
  <div class="mt-8 flex justify-end">
    <%= link_to "CSVエクスポート", patients_path(format: :csv, search: params[:search]), 
        class: "inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" %>
  </div>
</div>